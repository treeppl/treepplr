# Convert options to a proper string of flags, e.g., `_` to `-`, adding `--` in the beginning, spaces between things, etc.

Convert options to a proper string of flags, e.g., `_` to `-`, adding
`--` in the beginning, spaces between things, etc.

## Usage

``` r
options_to_string(options)
```
